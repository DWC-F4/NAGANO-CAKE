<div class="col-md-7">
<h2>マイページ</h2>
 	<h4>登録情報
 		<%= link_to "編集する",edit_customers_user_path(@user),class: "btn btn-primary" %>
  		<%= link_to "パスワードを変更する",edit_user_registration_path(@user),class: "btn btn-primary" %>
  	</h4>

  	<table class=table>

  			<th>氏名</th>
  			<td><%= @user.first_name %>&nbsp;<%= @user.last_name %></td>
  		</tr>

  		<tr>
  			<th>カナ</th>
  			<td><%= @user.first_name_kana %>&nbsp;<%= @user.last_name_kana %></td>
  		</tr>

  		<tr>
  			<th>郵便番号</th>
  			<td><%= @user.postal_code %></td>
  		</tr>

 		<tr>
  			<th>住所</th>
  			<td><%= @user.address %></td>
  		</tr>

 		<tr>
  			<th>電話番号</th>
  			<td><%= @user.phone_number %></td>
  		</tr>

 		<tr>
  			<th>メールアドレス</th>
  			<td><%= @user.email %></td>
    </tr>
  	</table>

  	<h4>配送先
 		<%= link_to "一覧を見る",customers_delivery_infos_path(@user),class: "btn btn-primary" %>
 	</h4>

 	<h4>注文履歴
  		<%= link_to "一覧を見る",customers_orders_path(@user),class: "btn btn-primary" %>
  	</h4>
</div>